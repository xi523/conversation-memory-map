interface:
  display_name: "Conversation Memory Map"
  short_description: "Sync discussion state and durable knowledge maps"
  brand_color: "#E04B43"
  default_prompt: "Use $conversation-memory-map to sync this conversation into a thinking map and a durable topic knowledge map."

policy:
  allow_implicit_invocation: false
